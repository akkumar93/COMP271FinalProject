#include "stdafx.h"
#include "Passenger.h"
#include <iostream>
#include <string>
#include <fstream>




using namespace std;

/*int Passenger::calc_distance(int a[], int b[]){
	distance = sqrt((b[0] - a[0]) ^ 2 + (b[1] - [1]) ^ 2);
	return distance;
}
int calc_cost(int dist){
	int total;
	total = 35 - (0.843)*(dist);
	return total;
}*/